namespace SupportSystem.Application.DTOs;

// Payload enviado para autenticação de usuários existentes.
public record LoginRequest(string Email, string Senha);namespace SupportSystem.Application.DTOs;

// Payload enviado para autenticação de usuários existentes.
public record LoginRequest(string Email, string Senha);
